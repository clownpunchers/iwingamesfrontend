import React from "react";

function SearchBox() {
  return (
    <input
      type="text"
      onChange={(e) => {
        
      }}
      className="searchbox"
      placeholder="search"
    />
  );
}

export default SearchBox;
